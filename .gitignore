# dependencies / build
node_modules
dist
.next
.turbo
*.tsbuildinfo

# env / secrets (dev defaults live in *.env.example)
.env
.env.local
.env.*.local

# os / editor
.DS_Store
.vscode/
.idea/

# logs / coverage
*.log
coverage

# infra / volumes
.volumes

# Claude Code local settings (permissions allowlist — user-specific)
.claude/settings.local.json

# Prisma dev SQLite (unused now, kept for safety)
apps/api/prisma/migrations/dev.db*

# Vite build
apps/web/dist

